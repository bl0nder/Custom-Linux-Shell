#include <stdio.h>
#include <errno.h>
#include <stdlib.h>
#include <string.h>


int main(int argc, char* argv[]) {
    
    // char* file = "";

    // if (!strcmp(argv[0], "-i")) {

    // }

    printf("%s %s %s %s", argv[0], argv[1], argv[2], argv[3]);
    return 0;
}